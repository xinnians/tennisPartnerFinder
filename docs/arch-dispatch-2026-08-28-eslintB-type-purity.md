# ESLint 恢復 Phase B 派工單：redundant constituents＋unnecessary assertion

- 日期：2026-08-28。母文件：
  `docs/arch-plan-2026-08-27-typeaware-eslint-restoration.md` §4B＋§5
  （方案一已由 Phase A 回報 §10.5 選定並經驗收核可）。
- 開工基準：`3893eae`（Phase A ACCEPTED）之後的最新 main HEAD（working
  tree 應乾淨，否則停手回報）。
- 沿用 Phase A 紀律：修法零 runtime token（逐檔 esbuild 擦除 raw 全等硬
  gate）、禁新增 `any`／`@ts-ignore`／`eslint-disable`（scoped override 是
  唯一核可的 config 層例外，見範圍一）、canary 三拍、超出裁決面＝停手回報。
- bundle 硬約束：total gzip 餘 1,435 B；預期淨 0 B，超 gate＝BLOCKED。
- 你不 commit、不 push；working tree 交驗收方。

## 範圍一：`eslint.config.js`

1. 恢復兩條（字串比對定位，非行號硬刪；現況 `no-redundant-type-constituents`
   `:84`／註解 `:83`、`no-unnecessary-type-assertion` `:86`／註解 `:85`）：
   恰刪四行，回 `recommendedTypeChecked` error（兩條在 8.67.0 皆 `'error'`，
   Phase A 已驗方法同源）。
2. **新增 generated scoped override**（plan §5 方案一）：ts 區塊之後新增
   一個 block，`files` 恰 `["src/data/databaseTypes.ts"]`、rules 恰
   `{"@typescript-eslint/no-redundant-type-constituents": "off"}`，附註解
   引用該檔 `:1` generated header（`npm run db:gen-types`）與 plan §5
   方案一；**不得**擴及其他檔或其他規則。其 2 筆 findings
   （`:1933`／`:1949`，'never' overridden）明確留存為記帳債。

## 範圍二：17 筆修復（manifest 開單凍結；動手前同法複驗，多一少一停手）

### redundant（非 generated 7 筆）——全是 `unknown` overrides union

| 位置 | 現況 |
| --- | --- |
| `src/app/App.tsx:48:53` | `Promise<unknown> \| unknown` 家族 |
| `src/controllerContracts.ts:17:40` | 同上 |
| `src/data/authApi.ts:26:12`／`:48:52`／`:55:55` | `unknown \| null` 家族（**非**頂層 `Promise<unknown> \| unknown`：`:26`／`:48` 是 `Promise<unknown \| null>` 回傳型別的內層 union，`:55` 是參數 `session: unknown \| null`；同因 unknown 吸收成員而冗餘，收斂後分別為 `Promise<unknown>`／`session: unknown`） |
| `src/sheets.ts:36:57` | `LoginModalOptions.onProvider` |
| `src/surfaceContracts.ts:42:57` | `LoginModalContentOptions.onProvider`（6C leaf） |

修法：`Promise<unknown> | unknown` 型 union 收斂為等價的 `unknown`
（union 本就坍縮為 `unknown`——這正是規則報它的原因；型別等價、public
surface 語意不變）。**同家族跨檔一致同步**（sheets／surfaceContracts／
App 的 onProvider 是同一 contract 家族，6C 驗收核過同形——收斂後仍須
同形，typecheck 在 configure 呼叫點自證）。純型別宣告變更，erased 零影響。

### assertion（10 筆）

`lifecycleActionsController:102:57`／`playerDirectoryController:103:57`／
`dataErrors:68:57`／`discoveryFeature:26:10`／`notificationFeature:164:20`／
`profileAuthFeature:85:17`／`:86:23`／`requestGate:58:13`／
`sessionStore:48:13`／`:112:36`。

修法（Phase A 回報 §10.5）：逐檔移除多餘 assertion／non-null token；若
移除後推導變寬導致 typecheck 紅，以 explicit annotation／generic／
`import type` 補回推導，**不順手簡化 expression、guard 或 default**。
注意 `sessionStore.ts:112` 鄰近 `syncCommit`（批 5 保留的載重同步點）——
只動 assertion token，該行 runtime 與訂閱語意零變更。emitter 括號差異
先調整寫法，仍不全等停手裁決。

## 探針與 canary（全部三拍逐字）

1. **規則 canary ×2**：修復完成 lint 綠後，逐條暫加最小違例（redundant：
   `export type C = string | unknown;`；assertion：`const s = "x" as string;
   export const t = s;` 類）→ lint 紅指名 rule ID→byte-identical 還原→綠。
2. **scoped override 載重 canary**：真 config lint 綠（databaseTypes 未
   被報）之後，暫時刪除 override block→lint 紅且恰報 `databaseTypes.ts`
   `:1933`／`:1949` 兩筆→byte-identical 還原→綠。證明 override 不是裝飾、
   也未遮到其他檔。

## 解凍清單（Q3 守則：未列即凍結）

- `eslint.config.js`：恰上述刪四行＋新增一個 scoped override block。
- manifest 上的 **13** 個 source 檔（redundant 非 generated 5＋assertion 8）：
  **僅型別層變更**（union 收斂／assertion 移除／必要的等價 annotation 補回）。
  註：多筆 assertion 的 `as X` token 位於報告行號之後 1–2 行（跨行
  statement，col 指向運算式起點）——該行找不到 `as` 屬正常，勿誤判座標錯。
- `src/data/databaseTypes.ts` **零 diff**（generated，方案一不動它）。
- 開單實測：零測試／importer／文件字面變更；發現需要動即停手回報。

**仍凍結**：其餘四條 off 規則；所有測試斷言語意；13 檔的全部 runtime
語意；`tsconfig`／`package.json`；bundle gate；`domainTypes.ts`（不在
manifest，若 union 收斂波及需其變更＝停手回報）。

## Ground truth（2026-08-28 開單實測；動手前自行重驗）

- 19 筆 manifest 如上（規則別 9＋10；檔別 redundant 6 檔／assertion 8 檔）。
- `databaseTypes.ts:1`＝`// Generated by \`npm run db:gen-types\`. Do not
  edit manually.`。
- 量化基準（Phase A 後，HEAD `3893eae`）：main gzip 187,466（餘 4,954 B）；
  total 257,627（餘 1,435 B）；unit 346；mock 298 passed／4 skipped；
  `window.__importAppModule` 110；type-aware 債現況＝6 條 off、283
  findings（本批後預期：4 條 off、264 findings＋databaseTypes 2 筆記帳債）。
- 已知存量 flake：`chat-settings-filters-smoke:468`（task_d6de363e）；
  test:local 污染紅依 guarded reset 三拍。

## 收尾標準矩陣（實跑、不接 pipe、逐字抄錄）

typecheck／lint（真 config，兩規則已恢復＋override 生效，全綠）／
prettier:check／build／check:production-bundle（淨值，超 gate＝BLOCKED）／
test:mock（≥298）／test:local（不豁免）／`git diff --check`／**13 檔逐檔
esbuild 擦除 raw 全等（無例外表）**／其餘四條 off 以暫時 enable 法重掃
對照（member-access 6／assignment 4／base-to-string 8／unbound-method
246；assertion 移除可能重新曝露 any 面——漂移逐筆解釋，上升尤其要說明）。

## 回報合約

寫 `docs/arch-dispatch-2026-08-28-eslintB-type-purity-report-codex.md`（不
commit、不 push），必含：config diff（刪四行＋override block 原文）、
17 筆逐筆修法表、union 收斂的家族同步說明、13 檔擦除對帳逐字、規則
canary ×2＋override 載重 canary 三拍逐字、四條 off 對照表、收尾矩陣逐字、
Codex 五問（第 5 問答「對 Phase C（unsafe-assignment 4＋member-access 6）
的建議——現存 10 筆的檔別分布與根因初判、與 Phase A 已收斂面的關係、
是否有需要行為批的位置」）、未做／疑義／BLOCKED。
