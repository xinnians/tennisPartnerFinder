# 派工單：獨立審查 2026-08-25 整體路線圖

- 日期：2026-08-25
- 審查對象：`docs/arch-roadmap-2026-08-25.md`（Claude 起草、使用者已拍板四項決策）
- 性質：**唯讀審查**，不改任何程式與文件；產出一份審查報告
- 立場要求：**對立審查**——你的任務是找出這份計畫會失敗的原因，
  不是背書。找不到問題也要說明你檢查了什麼、為什麼認為站得住。

## 背景（現況，2026-08-25）

- 架構優化管線批 0–2（含 2A–2D）、F0-9、F1R、F0-6／F0-4／文件批 D 全部 ACCEPTED；
  本機 HEAD `7a3facd`，**全部未 push**，origin 停在 `0be31a2`。
- qiuka.tw production 仍在跑 pre-React 舊版；REL checklist 剩 REL-10
 （穩定 preview 人工 QA）與 REL-11（QA fixtures 清理）兩項未完成。
- 使用者已拍板（審查時視為約束，不重議；但若有**證據級**異議，單獨列節供使用者重審）：
  1. 上線先行（push＋REL），加固批之後補；
  2. 批 3 切 3A（F3-0＋F3-1＋F3-3）→ 3B（F3-2）；
  3. 地圖批、bundle 拆分、長列表節流、守門收尾全部排入；
  4. 種子供給（REL-12）擱置。

## 開工前必讀（讀磁碟現行版本）

1. `docs/arch-roadmap-2026-08-25.md`（審查對象）
2. `docs/arch-dispatch-2026-08-22-frontend.md`（批 3／批 4 各項原始定義與依賴）
3. `docs/mvp-plan.md`（release checklist 一節）
4. `docs/architecture-decisions.md`（歷史決策索引，查計畫是否與既有裁決矛盾）
5. `docs/arch-reports/final-verdict-2026-08-21.md`（未盡事項）

## 審查項目（逐項回報）

### A. 事實查核（每條附指令＋實際輸出）

路線圖中的可驗證陳述逐條實查，至少涵蓋：

1. origin 是否確實停在 `0be31a2`、之後的 commit 數（`git rev-list --count`）。
2. REL-10／REL-11 是否確為 mvp-plan checklist 僅剩的未勾項。
3. 白箱直呼實測數（路線圖記 140，文件舊數 138）——重跑
   `grep -rhoE '__importAppModule\("[^"]*"\)' tests | wc -l` 確認。
4. F4-3「依賴批 2」與「排在 3B 後」的依據是否成立
  （母派工單原文 vs 路線圖的排序理由）。
5. 階段 1 三項（F4-6／F4-8／F4-2）在母派工單的定義與「隨時可做」標注是否相符。
6. 路線圖引用的 OPS 項（Site URL、referrer、推播 origin、migration 006–008
   矛盾待實查）在來源文件中是否確有其事。

### B. 排序與依賴邏輯

1. 各階段之間有沒有**遺漏的依賴**或**顛倒的順序**？特別檢查：
   地圖批（階段 4）建立在階段 1 已換 AdvancedMarker 之上——F4-1 的 diff 作法
   是否真的與新舊 marker API 無衝突？
2. 「守門收尾可插在任何階段之間」是否成立——F0-7（計數斷言清單化）與
   批 3 新增 surface 的互動有沒有順序問題？
3. 階段 0 步驟順序（push→CI 綠→REL QA→清 fixtures→merge main→換版→小 QA）
   有沒有更安全的排法或漏掉的步驟（例如 rollback 預演、備份時點）？

### C. 完整性（找漏）

1. 對照母派工單全部條目與 `docs/architecture-decisions.md`：有沒有
   **該排而沒排**、或**已裁決不做卻被排入**的項目？
2. 有沒有路線圖沒提、但上線前後會咬人的技術債？（提示方向：CSP Report-Only、
   reports.status 無法結案、WebKit 六條實機分類、`profiles.line_id` 凍結債——
   判斷它們該進哪個階段或明確標「不排」。）
3. 風險節五條之外，你認為最可能讓計畫失敗的前三名是什麼？各附依據。

### D. 異議（若有）

對已拍板四項決策若有證據級異議（不是偏好），單獨列節：
異議內容＋證據＋建議的替代方案＋改變決策的返工成本。

## 回報要求

寫成 `docs/arch-dispatch-2026-08-25-roadmap-review-report-codex.md`，
不提交、不 push。格式：

- 每條查核附**指令＋實際輸出**（不是「已確認」）；
- 技術陳述帶 [已驗證]／[推論]／[不確定] tag；
- 引用文件時附**該節原文第一句逐字**（防空殼引用）；
- 結論分四級：站得住／需小修（列 diff 建議）／有結構性問題（列具體段落）／
  異議需使用者重審；
- 檢查過而無問題的項目也要列出（讓「沒問題」可被驗證）。

## 紅線

- 唯讀：不改 `src/`、`docs/`（除了新增你的報告檔）、不跑會改狀態的指令、
  不 push、不重置 DB。
- 測試只允許唯讀性質的驗證指令（grep、git log、wc 等）；
  不需要跑完整測試矩陣。
