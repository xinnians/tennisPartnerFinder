# 首批改善驗證紀錄

日期：2026-09-09。範圍：G02／G03／G04；本文件保存第一階段本機 QA 快照，當時尚未部署。後續共用程式整理、遠端 CI 與正式部署結果見 [發布紀錄](batch-1-release-2026-09-09.md)。

## 使用者可見結果

- G02：匿名加入的登入說明區分符合程度的直接加入與需要審核的申請。
- G03：本人主揪卡新增「照這局再開」，帶入可編輯設定；必須重選日期時間，訂場重設為否，明確發布才建立新局。
- G04：詳情可複製公開球局摘要與既有深連結，明示最新名額／場地以連結為準；不包含私人備註、身分或成員資料。

## 環境與目標流程

Browser plugin not available：本 session 沒有 Browser plugin 的 `browser` skill，依 frontend-testing-debugging 指引使用既有 Playwright，未新增瀏覽器依賴。

- Mock：`http://127.0.0.1:5174`，desktop-chromium／mobile-chromium。
- Local：`http://127.0.0.1:5175`，既有本機 Supabase；重開流程明確測 1280×844 與 390×844。各 local project 序列執行，不重置資料庫。
- 地圖使用 repo Fake Maps；本機帳號使用測試 email/password，不能代表正式 Google／LINE OAuth。
- 目標：我的球局 → 我主揪的 → 照這局再開 → 選新日期時間 → 發布 → 新球局；另驗匿名加入提示與詳情複製摘要。

## 操作與畫面證據

| 檢查 | 證據與結果 |
| --- | --- |
| 頁面身分 | 既有測試導覽至 local URL，標題斷言為「球咖｜台北網球」 |
| 非空白 | 主揪卡、重開按鈕與表單提示可見 |
| 無 framework error overlay | 桌面／手機實際截圖檢視；無錯誤覆蓋畫面 |
| Console | 重開兩個 viewport 的 console.error／pageerror 收集均為空；不宣稱已檢查所有 console.warn |
| 版面 | 桌面／手機截圖檢視初始表單與卡片；主要操作可見，沒有內容互相遮蓋 |
| 發布守門 | 只選時間、未選新日期時，create_session RPC 呼叫數為 0 |
| 重開結果 | 發布恰好呼叫一次真實 RPC；保留 2.5–3.5 程度、2 個總缺額與可編輯文字，原局 booked、新局 walk_on |
| 焦點 | Escape 關閉表單後回到原「照這局再開」按鈕，且可再次開啟 |
| 摘要 | Mock 剪貼簿驗公開摘要、台北時間、球場與相容連結；unit 驗候選／關閉狀態及個資排除 |
| 帳號生命週期 | Unit 驗來源必須是本人主揪、既有草稿不覆蓋、帳號切換後舊表單不能發布 |

重開測試會產生 `/tmp/qiuka-growth-1280-my-sessions.png`、`/tmp/qiuka-growth-1280-draft.png` 及相同命名的 `390` 版本；draft 亦附在 Playwright test result。截圖為本機暫存證據，後續 session 可用下列命令重現，不將其視為永久檔案。

## 指令結果

| 指令 | 最終結果 |
| --- | --- |
| `npm run test:ci:frontend` | 通過，exit 0；seed／design sync、typecheck、lint、prettier、unit 759 passed／5 skipped、Chromium 384 passed／4 skipped、build、bundle 結構與 diff check |
| `npm run test:local` | 通過，exit 0；API 4 passed，Supabase Chromium 46 passed／12 skipped，包含桌面與 390px 重開旅程 |
| `npm run test:local:mobile` | 通過，exit 0；6 passed，涵蓋手機加入、邀請、表單與觸控操作 |
| `npm run check:production-bundle:release` | 通過，exit 0；嚴格 byte gate 0 exceeded |

Skipped 為現有 mode／viewport／Edge opt-in 等測試條件；本批未新增 skip，也不把跳過數算入通過數。未執行通知 Edge 專用整合組。

- 原有 byte 門檻未放寬：total JS raw 929,951／929,961 bytes，gzip 276,518／277,062 bytes；main raw 653,838／658,867 bytes。
- Raw total 僅餘 10 bytes，下一批前端工作需注意拆包或移除重複程式，不應直接提高門檻。
- 重現首批重開：`TENNIS_TEST_HARNESS_MODE=local npx playwright test --project=supabase-chromium --grep 'a host repeats'`。

## 本批曾發現並修正

- 第一版整合測試未切到「我主揪的」分頁，已修正測試入口。
- 重開按鈕原先經一般非同步 action runner 暫時 disable，導致表單關閉後無法恢復焦點；改為直接開啟表單，沿用控制器的既有表單去重，桌面／手機複驗通過。
- 初版 bundle 超過既有 raw total 門檻；共用新建表單預設值與既有程度／缺額格式化，精簡重複說明後通過嚴格檢查。

## 未測與限制

- 未部署、未執行正式站 OAuth、真實 Maps／實機剪貼簿或 Safari／WebKit；本次不是完整 hosted release gate。
- 零 migration，依 repo 規則豁免 `test:db`；未修改 RPC、RLS、伺服器生命週期或通知派送。本機測試沿用既有授權 RPC。
- 未實作分析事件、分享來源歸因或自動辨識重開來源；沒有真實留存數據，G01 試點仍未啟動。
- 未定案候選球局需重新選候選球場；現有本人摘要資料不足以直接重用完整候選清單。
- 已有的 Vite chunk 大於 500 kB 提示仍存在；專案正式結構與 byte gate 均以實際檢查結果為準。
